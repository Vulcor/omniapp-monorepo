### Develop

To be able to run local dev server a DB instance must be running in the background. For that run the following command:

```
docker-compose up -d
```

To develop all apps and packages, run the following command:

```
pnpm dev
```